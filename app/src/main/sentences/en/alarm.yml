set:
  - (set up?)|setup|start|create (a|an)? .name.? alarm for? .time.

cancel:
  - cancel|stop|disable|end|terminate the? .time. (alarm|alarms)
  - cancel|stop|disable|end|terminate the? .name.? (alarm|alarms) for? .time.

query_next:
  - when is? (the|my)? next? alarm (set|ready|for)?

query_all:
  - what (alarm|alarms) are? (set|ready|created)
  - when are? my? alarms

